#ifndef NUMBERS_H
#define NUMBERS_H
#include <stdio.h>
#include <time.h>

void prime_init();
int GetLargePrimeNumber();
int getCoprime(int n);
int GetRandomSign();
int GetRandomNumber(const int min, const int max);

#endif